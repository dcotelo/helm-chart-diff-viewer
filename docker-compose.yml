version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - /tmp:/tmp
    # Ensure Helm and Git are available in container
    # These should be installed in the Dockerfile

